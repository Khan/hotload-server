{
  "name": "hotloading-server",
  "version": "0.0.1",
  "description": "server to notify clients when files change",
  "files": [
      "dist/index.js"
  ],
  "dependencies": {
    "chokidar": "^1.0.5",
    "debounce": "^1.0.0",
    "express": "^4.10.2",
    "socket.io": "^1.3.6",
    "yargs": "^3.15.0"
  },
  "devDependencies": {
    "babel": "^5.8.3"
  },
  "scripts": {
      "build": "mkdir -p dist && babel src/ --out-file dist/index.js"
  }
}
